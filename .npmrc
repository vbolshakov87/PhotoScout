# Allow native module builds for these packages
# better-sqlite3 is required by promptfoo for local SQLite database
# esbuild is required for bundling

# Approved build scripts (required for native modules)
# See: https://pnpm.io/cli/approve-builds
onlyBuiltDependencies[]=better-sqlite3
onlyBuiltDependencies[]=esbuild
